export class Student {
    name: string;
    favoriteColor: string;
    favoriteNumber: string;

    constructor(name: string, favoriteColor: string, favoriteNumber: string) {
        this.name = name;
        this.favoriteColor = favoriteColor;
        this.favoriteNumber = favoriteNumber;
    }
}